import styled from "styled-components";

export const WrapperActionList = styled.div`
    display: flex;
    align-items: center;
    gap: 24px;
    font-size: 22px;
    cursor: pointer;
    padding-right: 24px;
`

export const IconContainer = styled.div`
    transition: all 0.3s ease;
    &:hover {
        color:  #878ff5;
    }
`